# Example: Coding Consultation

User: How do I use this API?

Expected response:

🚦
🟢 The API endpoint is `https://api.example.com/v2/users`.
🟢 It supports `GET` (list) and `POST` (create).
🟡 Authentication is usually Bearer Token in the `Authorization` header.
🔴 I am not sure about your company's exact permission configuration.
